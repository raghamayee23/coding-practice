from itertools import groupby

# Read input string
s = input().strip()

# Use groupby to compress consecutive characters
result = []
for char, group in groupby(s):
    count = len(list(group))   # number of consecutive occurrences
    result.append(f"({count}, {char})")

# Print the compressed string with spaces between tuples
print(" ".join(result))
